# 后台使用指南（给网站管理员）

网站分两部分：

- **前台**（给访客看）：打开网址就是，不用登录。
- **后台**（你自己用）：在网址后面加上 `/admin`，例如 `https://你的域名/admin`。

---

## 一、登录后台

1. 浏览器打开 `https://你的域名/admin`
2. 输入管理密码，点「登 录」
3. 密码由网站搭建者提供。忘了密码找搭建者，在 Cloudflare 控制台改。

登录一次管 12 小时，期间刷新不用重新输密码。点右上角「退出登录」可立即退出。

---

## 二、商品管理（最常用）

后台默认打开的就是「商品管理」页。

### 新增一个商品

1. 点右上角蓝色「＋ 新增商品」按钮，弹出编辑窗口
2. 填写内容：
   - **商品名称**（必填）：卡片上的标题
   - **分类**：下拉选择，选「（未分类）」则只出现在前台"全部"里
   - **价格**：随便写文字，比如 `¥199`、`面议`、`免费`，也可以留空
   - **商品描述**：支持图文混排（见下方"描述编辑器"说明）
   - **跳转链接**：访客在详情页点「了解详情 →」打开的网址，可留空
   - **商品图片**：点「上传图片」从电脑选图，上传成功会显示预览
   - **排序**：数字越小排越前面
   - **在网站上显示**：勾上才会在前台出现
3. 点右下角「保存」

### 描述编辑器（图文混排）

商品描述支持 Markdown 图文，访客点击商品卡片会打开详情页看到完整内容。

**插入图片（三种方式，任选）：**
- **拖拽**：把图片直接拖进描述编辑框，松手自动上传并插入
- **粘贴**：截图后直接在编辑框里 Ctrl+V 粘贴
- **按钮**：点工具栏的 🖼 按钮从电脑选图

**商品主图同样支持拖拽**：把图片直接拖到编辑窗口的图片预览框上即可替换主图。

**粘贴网页内容自动转格式**：从网页/Word 里复制带格式的内容（标题、加粗、列表、链接、图片）粘贴进描述框，会自动转成 Markdown，不用手动清理格式。直接复制 Markdown 源文本粘贴也一样支持。

**前台效果**：详情页顶部会把主图和描述里的所有图片做成轮播（左右箭头/圆点切换），点击任意图片可全屏放大查看（方向键翻页，Esc 关闭）。图片上会叠加站点名水印。

图片自动上传到你的存储桶，无需任何额外配置。

**常用格式（工具栏一键添加）：**
- **B** 加粗、**I* 斜体
- **H** 小节标题
- **•** 列表
- **🔗** 链接

**预览**：点编辑框下方的「预览」按钮，实时查看访客看到的效果，再点一次关闭。

描述文字可以写多长都行，换行、空行都保留。

### 修改商品

在商品列表里找到它，点「编辑」，改完点「保存」。

### 调整顺序

点「上移」「下移」，前台立刻按新顺序显示。

### 标记缺货

商品卖完了点「标记缺货」：前台该商品会**排到最后、图片蒙灰并显示"缺货"角标**（详情页也有标记），但仍然可见。补货后点「恢复有货」恢复原样。

### 暂时下架（不删除）

点「隐藏」，前台就看不到了（后台列表会显示"已隐藏"标记）。想恢复点「显示」。

### 删除商品

点「删除」→ 确认。**删了就找不回来**，不确定的话先用「隐藏」。

删除商品会**自动清理它上传过的图片**（主图和描述里的图），存储空间不积垃圾。唯一的例外：某张图同时被另一个商品使用时，那张图会保留下来，不会影响别的商品。

---

## 三、分类管理

顶部切到「分类管理」：

- **添加分类**：输入名字点「添加分类」
- **重命名**：点「重命名」，输入新名字
- **删除分类**：分类里的商品不会被删，会变成"未分类"

分类的展示顺序和添加顺序一致，数字越小越靠前（后台暂不提供分类排序按钮，需要调整找搭建者）。

---

## 四、站点设置

顶部切到「站点设置」：

- **网站名称**：导航栏和浏览器标签页显示的名字
- **公告内容**：前台顶部公告区文字。换行有效；直接粘贴网址（https:// 开头）会自动变成可点击的链接

改完点「保存设置」。

---

## 五、主题切换与公告

前台和后台右上角都有 🌓 按钮，点击在 **跟随系统 → 亮色 → 暗色** 之间循环。选"跟随系统"时自动匹配设备的深色模式，选择会记住。

**公告**：访客点导航栏的「公告」按钮弹窗查看，点 × 或按 Esc 关闭。公告内容在后台「站点设置」里编辑。

## 六、常见问题

**上传图片失败？**
图片得是 JPG / PNG / GIF / WebP 格式，且不超过 10MB。手机拍的图一般没问题。

**改了东西前台没变？**
刷新一下前台页面（Ctrl+F5 强制刷新）。图片有缓存，新商品不受影响。

**不小心把密码忘了 / 想换密码？**
找搭建者，在 Cloudflare 控制台 → 你的 Pages 项目 → Settings → Environment variables 里改 `ADMIN_PASSWORD`，然后重新部署一次。

**会话已过期，被踢回登录页？**
正常现象，12 小时自动过期，重新输密码登录即可。
